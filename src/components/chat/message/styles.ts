import { createStyles } from "antd-style";

export const useIconStyles = createStyles(({ css }) => ({
	userReplyButton: css`
		background-color: var(--background-color);
	`,
	aiReplyButton: css`
		background-color: var(--background-color);
	`,
}));